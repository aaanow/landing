# Database
DATABASE_URI=postgresql://payload:payload@localhost:5432/payload

# Payload Secret - generate with: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-key-here

# Seed API Key - required for /api/seed-* and /api/reset-password routes
# Generate with: openssl rand -hex 32
SEED_API_KEY=
